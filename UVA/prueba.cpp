#include<iostream>
#include<stdio.h>
#include<cmath>
#include<unordered_map>

using namespace std;

int main(){
	string line;
	int num1;
	cin>>num1;
	while(getline(cin,line))
		cout<<line<<endl;
	while(getline(cin,line))
		cout<<line<<endl;
	while(getline(cin,line))
		cout<<line<<endl;
	while(getline(cin,line))
		cout<<line<<endl;
	return 0;
}
