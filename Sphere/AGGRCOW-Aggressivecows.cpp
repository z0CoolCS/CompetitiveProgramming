#include<bits/stdc++.h>

using namespace std;

#define REP(i,a,b) for(int i=a;i<b;i++)

int n,c;
int arr[100002];

bool verificate(int x){
  int pos=arr[0],int cows=1;
  REP(i,1,n){
  }
}
int mindistance(){
  int start=arr[0],end=arr[n-1];
  while(start<end){
    int mid=(start+end)>>1;
    
  }
}

int main(){

	int t;
	cin>>t;
	while(t--){
		cin>>n>>c;

		REP(i,0,n)
			cin>>arr[i];
		
		sort(arr,arr+n);
	}

	return 0;
}
